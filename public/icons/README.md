# App icons — TODO

Add real PWA icons here before shipping (the manifest references them):

- `icon-192.png` — 192×192
- `icon-512.png` — 512×512 (maskable-safe padding)

Until these exist, the app still runs in the browser; only "Add to Home Screen"
install will warn about missing icons.
